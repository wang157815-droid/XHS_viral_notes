# 基础依赖
PyExecJS>=1.5.1
requests>=2.31.0
# 小红书签名库（阶段 4.1 hotfix 3 引入,跟进最新签名算法）
# 原 static/xhs_xs_xsc_56.js 是 2024 年版本,算法已被平台迭代过,
# 签名虽能通过"success=True"校验,但服务端内容风控会识别算法版本返回空结果(items=[])。
# 切换到 xhshow 后诊断脚本验证能采到真实数据。
# 如需回退老算法,设置 XHS_SIGN_BACKEND=execjs
xhshow==0.2.0
loguru>=0.7.2
python-dotenv>=1.0.0
retry>=0.9.2
openpyxl>=3.1.2
nest-asyncio>=1.5.8

# Web框架
fastapi>=0.104.1,<0.115.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
python-multipart>=0.0.6
aiofiles>=23.2.1

# 认证相关
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1  # 锁定版本，4.1+与passlib不兼容

# 数据分析
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
jieba>=0.42.1
openai>=1.0.0
wordcloud>=1.9.3
Pillow>=10.0.0

# RAG和知识库相关
# 注意：chromadb与paddlepaddle存在protobuf版本冲突
# 如果需要同时使用OCR和RAG功能，请参考文档说明
pypdf>=3.17.0
python-docx>=1.1.0
markdown>=3.5.0

# ChromaDB单独安装（可选 - 用于RAG功能）
# pip install chromadb
#
# 或手动安装兼容版本：
# pip install "protobuf>=3.20.0,<3.21.0" "pyyaml==6.0.2" chromadb

# OCR相关（可选，单独安装）
# 方案A（推荐）：EasyOCR - 兼容性好，易安装
# pip install easyocr
#
# 方案B：PaddleOCR - 功能强大，但可能有兼容性问题
# pip install paddlepaddle paddleocr

# 音画同步分析相关
aiohttp>=3.9.0
httpx>=0.27.0
dashscope>=1.20.0

# 扫码登录相关（Playwright 方式）
# 安装后需执行: playwright install chromium
playwright>=1.40.0

# 阶段4 编排引擎（LangGraph 企业级状态图）
# 锁 minor 防大版本破坏 API
langgraph>=0.2.0,<0.3.0

# ============================================================
# 阶段 4.α 基础设施（Redis 缓存 + ARQ 队列 + pgvector 笔记向量）
# ============================================================
# Redis 客户端(async)：ARQ broker + L1 热缓存 + SSE backlog
redis>=5.0.0
# ARQ：async-first Redis 任务队列（队列 + cron 一体,替代 Celery+Beat）
arq>=0.25.0
# PostgreSQL 异步驱动（仅用于 pgvector 笔记向量存储,业务表仍 JSON 等 4.6 再迁）
asyncpg>=0.29.0
# PostgreSQL 同步驱动 + 迁移工具（阶段 4.6 业务表硬切）
psycopg[binary]>=3.1.18
alembic>=1.13.0
# pgvector Python 客户端（向量列类型绑定 + SQLAlchemy Type）
pgvector>=0.2.5
# SQLAlchemy 2.0 异步引擎（pgvector 操作底座）
sqlalchemy[asyncio]>=2.0.25