[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    deterministic: stable eval checks suitable for PR gates
    nightly: longer or external-service eval checks for scheduled runs
    requires_llm: eval checks that require real model providers
