# 多用户使用指南

> 本文档面向产品经理及团队负责人，说明系统的多用户支持能力、限制及最佳实践。

---

## 支持人数说明

本系统已实现完整的多用户认证与数据隔离架构，但部分底层资源仍为全局共享，因此**并发能力有上限**。

| 使用场景 | 推荐人数 | 说明 |
|----------|----------|------|
| 同时在线浏览/查看历史 | **≤ 10 人** | 仅读取操作，无资源竞争 |
| 同时执行采集+分析任务 | **≤ 3 人** | 受 SQLite 写入锁、API 限流、视频缓存竞争制约 |
| 同时扫码登录 | **1 人**（可视化） / **5 人**（API） | Playwright 浏览器单例限制 |

---

## 多用户隔离机制

系统已实现以下隔离保障：

- **独立数据目录**：每用户拥有独立目录 `datas/users/{username}/`，分析结果、Excel报告、Cookie 互不干扰
- **Cookie 独立存储**：每用户 Cookie 单独管理，支持主/备双 Cookie
- **任务归属校验**：任务创建时绑定用户，操作前验证归属权限（管理员可越权查看）
- **JWT 认证隔离**：所有 API 接口需要 Token 认证，24小时自动过期

---

## 并发限制与瓶颈

以下资源为全局共享，是并发上限的主要制约因素：

| 共享资源 | 影响 | 缓解建议 |
|----------|------|----------|
| SQLite `tasks.db` | 多人同时写入任务状态可能阻塞 | 避免 3+ 人同时启动新任务 |
| 视频缓存目录 | 缓存键按 `note_id` 而非用户区分 | 避免多人同时分析相同笔记 |
| API 健康追踪器 | 一人触发限流 → 全员显示"降级" | 正常现象，等待冷却即可 |
| 扫码登录浏览器 | 可视化模式同时仅 1 个会话 | 错开登录时间，或使用 API 扫码模式（5并发） |
| AI 大模型 API | 共享同一个 API Key 的配额 | 关注 Token 消耗，高峰期可能排队 |

---

## 最佳实践

1. **错开使用**：多人同时启动大批量采集（100+ 条视频笔记）容易触发小红书反爬，建议错开 10-15 分钟
2. **避免重复分析**：系统不会自动去重不同用户对同一关键词的采集，手动协调可减少浪费
3. **管理员监控**：管理员可查看所有用户的任务状态，发现异常及时暂停
4. **Cookie 独立配置**：每个用户使用自己的小红书账号 Cookie，不要共用同一个
5. **登录排队**：如遇"扫码会话已满"，等待其他用户完成登录后重试

---

## 扩容方向（如需支持更多用户）

如需支撑 **10+ 人**同时使用，建议进行以下升级：

| 升级项 | 说明 |
|--------|------|
| SQLite → PostgreSQL | 启用 WAL 模式或迁移到 PostgreSQL，解决写入锁瓶颈 |
| 缓存键增加用户前缀 | 视频/封面缓存按用户隔离，避免相互覆盖 |
| 任务状态持久化 | 从内存 dict 迁移到数据库，支持多进程共享 |
| HealthTracker 按用户实例化 | 避免一人限流影响所有人 |
| 多实例 + 负载均衡 | 部署多个服务实例，Nginx 反向代理分发请求 |

---

## 一句话总结

> **当前版本适合 3-5 人小团队日常使用**，浏览查看可支持 10 人。如需更大规模，请参考扩容方向进行架构升级。
